# Core bot framework (async, long polling)
aiogram>=3.0,<4.0

# Async HTTP for OpenRouter + link fetching
httpx==0.27.2

# Typed settings loaded from env / .env
pydantic-settings==2.5.2

# Context file parsing
pypdf==5.0.1
python-docx==1.1.2

# Main-content extraction from web pages (links)
trafilatura==1.12.2

# Output generators
python-pptx==1.0.2   # PPTX slide decks
fpdf2==2.8.7         # Unicode/Cyrillic PDFs (uses system DejaVu font)

# Clean Telegram rendering: markdown -> (text, entities), rendered to Telegram
# HTML via aiogram's html_decoration (no fragile regex, Unicode-safe).
telegramify-markdown==1.2.0

# Persistence + secrets
asyncpg==0.30.0      # async PostgreSQL driver (no ORM)
cryptography==43.0.1 # Fernet encryption for bring-your-own OpenRouter keys
